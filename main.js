import { createApp } from 'vue'
import App from './src/App.vue'
import './style.css'

console.log('👀 main.js -> initialization', { message: 'Starting Vue.js SVG Animation Showcase' });

createApp(App).mount('#app')